{
  "name": "zwave-test",
  "version": "1.0.0",
  "author": "Rob Huehn <rob@theatersoft.com>",
  "scripts": {
    "BUS": "BUS=`[ -f .bus ] && cat .bus` AUTH=`[ -f .auth ] && cat .auth`",
    "DEBUG": "npm run BUS -- node --inspect --debug-brk",
    "RUN": "npm run BUS -- node",
    "add": "npm run BUS -- node add.js",
    "healNetwork": "npm run BUS -- node healNetwork.js",
    "on": "npm run BUS -- node on.js",
    "off": "npm run BUS -- node off.js",
    "remove": "npm run BUS -- node remove.js",
    "test": "npm run BUS -- node --harmony-async-await test.js"
  }
}
